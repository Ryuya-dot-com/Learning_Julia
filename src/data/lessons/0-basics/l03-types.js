// レッスン: データの型 — 整数・小数・文字列・真偽値と数値安定性
// num は書かない（セクション順+ファイル名順から自動採番される）
export default {
  id: "data-types",
  title: "データの型",
  tag: "整数・小数・文字列・真偽値と計算精度",
  pages: [
    {
      t: "データには「種類」がある",
      b: [
        "Juliaのデータには型(かた)と呼ばれる種類があります。`typeof` 関数で、どの型なのか調べられます。",
      ],
      code: `println(typeof(3))
println(typeof(3.5))
println(typeof("こんにちは"))
println(typeof(true))`,
      out: `Int64
Float64
String
Bool`,
    },
    {
      t: "覚えておきたい4つの型",
      b: [
        "`Int64` は整数、`Float64` は小数、`String` は文字列、`Bool` は `true`(真)と `false`(偽)の2つだけをとる真偽値です。",
        "同じ「3」でも、`3` は整数、`3.0` は小数、`\"3\"` は文字列。見た目が似ていても、型はちがいます。",
      ],
    },
    {
      t: "型を知るとエラーに強くなる",
      b: [
        "たとえば、文字列と数値はそのままでは足せません。",
      ],
      code: "\"3\" + 3",
      out: "ERROR: MethodError: no method matching +(::String, ::Int64)",
      err: true,
      a: [
        "「String型 と Int64型 を + でつなぐ方法が見つからない」という意味のエラーです。エラーメッセージには型の名前がよく登場するので、型を知っていると原因がすぐに見つけられます。",
        "実際の画面では、この後に候補の一覧など長い続きが表示されます。ぜんぶ読む必要はありません。まずは1行目だけ読めば十分です。",
      ],
    },
    {
      t: "Float64は、実数を有限個の点で近似する",
      b: [
        "`Float64` は小数を無限の精度で保存する型ではありません。符号・指数・仮数を合わせた64 bitで実数を近くの表現可能値へ丸めます。0.1や0.2は2進数では有限桁にならないため、足し算の結果に小さな差が残ります。",
        "これはJulia固有の不具合ではなく、IEEE 754の倍精度浮動小数点を使う多くの言語に共通します。通常の統計計算ではFloat64が速く十分正確ですが、等値比較、極端な確率、ほぼ等しい数の差では計算方法を意識します。",
      ],
      code: `x = 0.1 + 0.2
println(x)
println(x == 0.3)
println(isapprox(x, 0.3))
println(eps(1.0))
println(nextfloat(0.0))`,
      out: `0.30000000000000004
false
true
2.220446049250313e-16
5.0e-324`,
      a: [
        "`eps(1.0)` は1.0付近で隣り合うFloat64の間隔、`nextfloat(0.0)` は0より大きい最小のFloat64です。数の大きさによって間隔は変わるので、すべての値へ同じ絶対誤差を想定しません。",
        "数値テストでは目的に合う `atol`／`rtol` を決めて `isapprox` を使います。ただし、カテゴリIDや試行数のように本来厳密な整数まで『だいたい同じ』で済ませるものではありません。型と意味を合わせます。",
      ],
    },
    {
      t: "数学的に同じ式でも、計算順序でunderflowする",
      b: [
        "ご指摘の例では、`0.01^200 = 10^-400` がFloat64で表せる最小の正数より小さく、先に累乗すると0.0へunderflowします。その後で対数を取ると `log(0.0) == -Inf` です。先に対数へ移せば、積の対数を和として有限値のまま計算できます。",
        "尤度は多数の小さな確率の積なので、統計モデルでは特にこの問題が起こります。尤度そのものを掛け続けず、対数尤度を `sum(log.(p))` として足し合わせるのが基本です。",
      ],
      code: `probability = 0.01
n = 200

product_first = log(probability^n)
log_first = n * log(probability)
audited = setprecision(BigFloat, 256) do
    log(big"0.01"^200)
end

println(probability^n)
println(product_first)
println(round(log_first, digits = 3))
println(round(Float64(audited), digits = 3))
println((isfinite(product_first), isfinite(log_first)))`,
      out: `0.0
-Inf
-921.034
-921.034
(false, true)`,
      a: [
        "`-Inf` は『真の確率が厳密に0』という証拠ではありません。この例では途中結果が表現範囲を下回った警告です。`isfinite` で検出し、行を黙って削除したり0へ置換したりせず、式をlog空間へ直します。",
        "`BigFloat` は監査に使えますが、まず安定な式へ変形する方が速く再利用しやすい解決です。また `BigFloat(0.01)` は既に丸めたFloat64を高精度化するだけなので、10進文字列から直接作る `big\"0.01\"` を使っています。",
      ],
    },
    {
      t: "表示の丸めと、計算上の判定を分ける",
      b: [
        "画面や論文に表示する丸め値を、次の計算へ戻してはいけません。p=0.04996は丸める前なら0.05未満ですが、小数第3位へ丸めた0.050を使うと判定が変わります。比較、区間計算、モデル選択には未丸め値を使い、丸めは最後の表示だけにします。",
        "ただし機械精度ほど小さい差で科学的結論が反転するなら、コードだけでなく結論自体が境界に敏感です。効果量・信頼区間・代替仕様を示し、『有意／非有意』の二分だけへ強い意味を持たせません。",
      ],
      code: `p = 0.04996
shown = round(p, digits = 3)
println((raw = p < 0.05,
         rounded = shown < 0.05,
         shown = shown))

println((1e16 + 1.0) - 1e16)
println((1e16 - 1e16) + 1.0)`,
      out: `(raw = true, rounded = false, shown = 0.05)
0.0
1.0`,
      a: [
        "浮動小数点の加算は結合法則を厳密には満たしません。並列処理や集計順の違いで最下位桁が変わることがあります。再現性検証では表示文字列の完全一致だけでなく、科学的に必要な許容誤差と不変条件をテストします。",
        "`isapprox` は数値実装の比較に役立ちますが、p値の閾値を曖昧にする関数ではありません。計算誤差への許容と、研究上の意思決定規則を別々に設計します。確率分布編で、裾確率と対数尤度の安定なAPIへ進みます。",
      ],
    },
  ],
  ex: [
    {
      k: "choice",
      q: "`typeof(2.5)` の結果はどれでしょう?",
      opts: [
        "Float64",
        "Int64",
        "String",
      ],
      ans: 0,
      why: "小数点のつく数は `Float64`(浮動小数点数)という型になります。",
      hint: "2.5 は整数でしょうか、小数でしょうか?",
    },
    {
      k: "choice",
      q: "`typeof(\"123\")` の結果はどれでしょう?",
      opts: [
        "String",
        "Int64",
        "Bool",
      ],
      ans: 0,
      why: "数字に見えても、`\"` で囲まれていれば文字列(`String`)です。この \"123\" のままでは計算できません。",
      hint: "`\"` で囲まれているかどうかに注目しましょう。",
    },
    {
      k: "fill",
      q: "`true` と `false` の2つだけをとる型の名前を入力しましょう。",
      accept: [
        "bool",
      ],
      show: "Bool",
      why: "真偽値の型は `Bool` です。Juliaで書くときは先頭が大文字の B になる点に注意しましょう。「条件分岐」で大活躍します。",
      hint: "英語の boolean(ブーリアン)を短くした名前です。",
      placeholder: "型の名前",
    },
  ],
};
