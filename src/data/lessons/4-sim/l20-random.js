// レッスン: 乱数・標本抽出・再現性 — ランダムなのに、やり直せる
// 乱数の具体値には依存せず、Julia Random標準ライブラリの公開APIと性質だけを使う。
export default {
  id: "randomness-reproducibility",
  title: "乱数・標本抽出・再現性",
  tag: "RNGを明示して、抽出と割付を分ける",
  pages: [
    {
      t: "乱数はどこから来るか",
      b: [
        "コンピュータの疑似乱数は、RNG(乱数生成器)の状態から決定的な計算で作る、ランダムに見える数列です。初期状態を決める値が種(シード)です。同じRNG・同じ種・同じ呼び出し順なら同じ列を作れるため、ランダム化と再現性を両立できます。",
        "Juliaでは `rng = Xoshiro(2026)` のようにRNGを値として作れます。以後 `rand(rng, ...)` の第1引数へ明示して渡します。グローバルな `Random.seed!` だけに頼るより、どの処理がどの乱数列を消費したか追いやすく、関数の外側へ影響を漏らしません。",
        "「確率変数と確率分布」で作った分布オブジェクトも、このRNGへ渡せます。分布は何を生みやすいか、RNGは今回どの疑似乱数列を使うか、`rand` は両者から標本を生成する操作です。",
      ],
    },
    {
      t: "同じrandで、集合や分布から抽出する",
      b: [
        "`rand(rng)` は0以上1未満の一様乱数、`rand(rng, 1:6)` は1から6の集合から1個を選びます。`rand(rng, d, n)` は分布 `d` から `n` 個を生成します。Juliaの多重ディスパッチにより、同じ関数名を抽出元の種類に合わせて使えます。",
      ],
      code: `using Random, Distributions

rng = Xoshiro(2026)
d = Normal(500, 50)

u = rand(rng)                  # 0以上1未満から1個
die = rand(rng, 1:6)           # 1:6から1個
rt = rand(rng, d, 20)          # Normal(500, 50)から20個
correct = rand(rng, Bernoulli(0.8), 20)

println(0 <= u < 1)
println(die in 1:6)
println(length(rt))
println(all(x -> x in (0, 1), correct))`,
      out: `true
true
20
true`,
      a: [
        "`randn(rng, 20)` は標準正規分布から20個を引く便利な短縮形です。一方、`rand(rng, Normal(500, 50), 20)` は平均やSDを分布オブジェクトに保持でき、`cdf` などの確率計算と同じ対象を共有できます。以後は生成過程が見える後者を中心に使います。",
        "出力は乱数の具体値ではなく性質を検査しています。Juliaやパッケージの更新で列が変わっても、指定した分布とサポートは同じでなければなりません。",
      ],
    },
    {
      t: "標本抽出と無作為割付は違う",
      b: [
        "標本抽出は「母集団から誰を研究へ入れるか」を決めます。無作為割付は「研究に入った人をどの条件へ置くか」を決めます。前者は標本の代表性、後者は条件間の比較可能性に関わります。片方をランダム化しても、もう片方を自動的に満たしたことにはなりません。",
      ],
      code: `using Random

rng = Xoshiro(7)
population = collect(1001:1100)
sample_ids = shuffle(rng, population)[1:10] # 母集団から10人を抽出

labels = repeat(["control", "treatment"], 5)
assignment = shuffle(rng, labels)            # 10人を条件へ割付

println(length(unique(sample_ids)) == 10)
println(count(==("control"), assignment))`,
      out: `true
5`,
      a: [
        "この例の抽出は重複なしです。復元抽出が必要な場面では、選ばれた要素を候補へ戻す仕組みが必要です。「推定と不確かさ」でブートストラップとして扱います。",
        "`shuffle(rng, labels)` は同数のラベルを混ぜるので、5人ずつを保った割付になります。各人を独立なコイントスで割り付ける方法とは、最終人数を固定するかどうかが違います。",
      ],
    },
    {
      t: "刺激順序とカウンターバランス",
      b: [
        "全員に同じ刺激順序を見せると、順序の効果が条件と重なることがあります。一方、毎回その場で混ぜて記録を残さなければ、異常値や中断をあとから追えません。参加者ごとに独立なRNGを作り、生成した割付表そのものも保存します。",
      ],
      code: `using Random

function order_for(participant, trials)
    rng = Xoshiro(10_000 + participant)
    return shuffle(rng, trials)
end

trials = repeat(["cong", "incong"], 4)
order_a = order_for(2, trials)
order_b = order_for(2, trials)

println(order_a == order_b)
println(trials == repeat(["cong", "incong"], 4))`,
      out: `true
true`,
      a: [
        "同じ参加者番号なら同じ順序を再生成でき、`shuffle` は元の `trials` を変更しません。元を変更する `shuffle!` とは区別します。",
        "ただし、参加者IDをそのまま公開用シードにすると識別情報との対応が漏れることがあります。実務では研究用の連番など個人情報でないキーを使い、最終的な `participant × trial × condition × stimulus` の表をCSV等へ保存します。",
      ],
    },
    {
      t: "再現性は種だけではない",
      b: [
        "同じ種でも、途中で `rand` を1回追加すれば、その後の列は1つずつずれます。Juliaやパッケージの更新でアルゴリズムが変わることもあります。したがって種は必要ですが、将来の完全再現を単独で保証しません。",
        "研究で残すものは、種とRNGの種類、Juliaの `VERSION`、`Project.toml` と `Manifest.toml`、乱数を呼ぶコード、そして実際に使った割付表や生成済みデータです。完全に同じ列が必要なら、再生成だけに賭けず結果そのものを保存します。",
        "テストも具体的な乱数列の一致より、長さ、サポート、平均が十分な反復で理論値へ近づくことなど、必要な性質を検査します。これで実装改善に強く、統計的な誤りには敏感な検証になります。",
      ],
      code: `using Random

rng1 = Xoshiro(2026)
rng2 = Xoshiro(2026)

same_call_order = rand(rng1, 5) == rand(rng2, 5)
println(same_call_order)
println(VERSION) # 報告時はこの値も記録する`,
      out: `true
1.12.6`,
    },
  ],
  ex: [
    {
      k: "choice",
      q: "無作為抽出と無作為割付の説明として正しいのはどれでしょう?",
      opts: [
        "抽出は誰を標本へ入れるか、割付は標本内の誰をどの条件へ置くかを決める",
        "抽出と割付は同じ操作の別名である",
        "無作為割付をすれば、便宜抽出した標本も母集団を必ず代表する",
      ],
      ans: 0,
      why: "抽出は研究へ入る人、割付は研究内の条件を決めます。割付は条件比較を助けますが、標本の代表性を自動的には改善しません。",
      hint: "母集団→標本と、標本→条件という2つの矢印を分けます。",
    },
    {
      k: "fill",
      q: "明示的なRNG `rng` を使い、分布 `d` から `n` 個を生成します。空欄〔?〕に入る引数を入力しましょう。",
      code: `x = rand(〔?〕, d, n)`,
      accept: ["rng"],
      show: "rng",
      why: "`rand(rng, d, n)` と第1引数へRNGを渡すと、乱数状態への依存がコード上に現れます。",
      hint: "`Xoshiro(2026)` を代入した変数名です。",
      placeholder: "RNGの変数名",
    },
    {
      k: "tf",
      q: "乱数と再現性について、それぞれ正しいか判定しましょう。",
      items: [
        {
          s: "同じRNG・種・呼び出し順なら、同じ環境で同じ疑似乱数列を再生成できる",
          a: true,
          why: "疑似乱数はRNGの状態から決定的に作られます。ただし呼び出し順が変われば、その後に使う状態も変わります。",
        },
        {
          s: "種を固定しておけば、Juliaやパッケージを更新しても将来にわたって同じ具体列が保証される",
          a: false,
          why: "乱数生成や上位関数のアルゴリズムは更新され得ます。バージョンとManifestを記録し、完全一致が必要な生成済みデータや割付表は保存します。",
        },
        {
          s: "実際に使った割付表を保存することは、種だけを記録するより強い再現性の証拠になる",
          a: true,
          why: "割付表は、その時点で実際に使った条件と順序を直接記録します。コードや乱数アルゴリズムが後で変わっても照合できます。",
        },
      ],
      hint: "種、ソフトウェア環境、生成結果の保存がそれぞれ何を保証するか分けましょう。",
    },
  ],
};
