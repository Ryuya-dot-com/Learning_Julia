// レッスン: 確率変数と確率分布 — 分布をJuliaの値として操作する
// APIは Julia Random 標準ライブラリと Distributions.jl の公開APIに限定する。
// 乱数の具体値には依存せず、理論値または性質だけを出力する。
export default {
  id: "probability-distributions",
  title: "確率変数と確率分布",
  tag: "分布をつくり、確率を安定に計算し、乱数を引く",
  pages: [
    {
      t: "ヒストグラムの背後にあるもの",
      b: [
        "「探索の可視化」で描いたヒストグラムは、手元にある有限個のデータの形です。これを経験分布と呼びます。一方、まだ観測していない人も含めて、データがどの値をどれくらい生みやすいかを表すモデルが確率分布です。標本のヒストグラムそのものが母集団分布なのではありません。",
        "このレッスンでは、Juliaで分布を1つの値として作ります。作った値に対して平均を尋ね、ある範囲の確率を計算し、乱数を発生させます。数式、確率計算、シミュレーションが同じオブジェクトにつながるのが、Juliaで分布を学ぶ強みです。",
        "まず言葉を分けましょう。確率変数は、生成結果を数値へ対応させる規則です。確率分布は、その変数がどの値をどれくらい取りやすいかを記述します。観測値 `512.5` は分布ではなく、分布から生じた可能性のある1つの値です。",
      ],
    },
    {
      t: "取りうる値から候補を絞る",
      b: [
        "分布を選ぶ最初の質問は「この変数は、どんな値を取りうるか」です。この範囲をサポートと呼びます。正誤なら0/1、正答数なら0から問題数まで、一定時間のエラー数なら0以上の整数、反応時間なら正の連続値です。",
        "ただし、サポートが同じだけでは分布は決まりません。正の連続値でも、左右対称に近いことも、右に長い裾を引くこともあります。変数の型だけでなく、値を生み出す過程と実際の可視化を合わせて判断します。",
      ],
      code: `using Distributions

correct = Bernoulli(0.8)       # 1試行の正誤: 0か1
score   = Binomial(20, 0.8)    # 20試行中の正答数: 0〜20
errors  = Poisson(2.5)         # 一定時間の事象数: 0以上の整数
rt      = LogNormal(log(500), 0.25) # 正で右裾を引く連続値

println(insupport(correct, 1))
println(insupport(score, 21))
println(insupport(rt, -10))`,
      out: `true
false
false`,
      a: [
        "`Bernoulli(0.8)` の `0.8` は1になる確率、`Binomial(20, 0.8)` の `20` は試行数です。`Poisson(2.5)` の `2.5` は区間あたりの平均発生数です。",
        "`LogNormal(log(500), 0.25)` の引数は元の尺度の平均・SDではなく、対数を取った尺度の位置と広がりです。この例の中央値は500です。分布ごとに引数の意味が違うため、名前だけで推測せずドキュメントを確認します。",
      ],
    },
    {
      t: "分布を値として作り、乱数を引く",
      b: [
        "`Distributions.jl` はJulia本体とは別のパッケージです。未導入ならREPLのパッケージモードで `] add Distributions` を一度実行し、コードでは `using Distributions` と読み込みます。`Random` と `Statistics` は標準ライブラリです。",
        "`Normal(500, 50)` を変数 `d` に入れると、平均500・標準偏差50の正規分布が1つの値になります。`mean(d)` や `std(d)` で理論上の性質を尋ね、`rand(rng, d, 5)` でその分布から5個の乱数を発生させられます。",
      ],
      code: `using Random, Statistics, Distributions

d = Normal(500, 50)
rng = Xoshiro(2026)
x = rand(rng, d, 5)

println(mean(d))
println(std(d))
println(length(x))
println(all(isfinite, x))`,
      out: `500.0
50.0
5
true`,
      a: [
        "ここでは乱数の具体的な5値を表示していません。乱数アルゴリズムの更新で列が変わっても、5個が生成され、すべて有限値であるという教材上の性質は変わらないからです。",
        "`rand(rng, d)` なら1個、`rand(rng, d, 1000)` なら1000個です。分布 `d` と個数を変数にできるので、同じ関数を正規分布、二項分布、ポアソン分布へ使い回せます。",
      ],
    },
    {
      t: "確率を関数で尋ねる",
      b: [
        "離散分布では `pdf(d, x)` が値 `x` の確率を返します。連続分布では同じ関数名が確率密度を返しますが、密度の高さは一点の確率ではありません。連続変数がちょうど `550.000…` になる確率は0で、範囲の確率を面積として考えます。",
        "`cdf(d, x)` は `x` 以下となる確率、`ccdf(d, x)` は `x` より大きくなる確率です。`quantile(d, p)` は累積確率 `p` に対応する境界値を返します。これらは分布の種類が変わっても同じ名前で使えます。",
      ],
      code: `using Distributions

d = Normal(500, 50)
println(round(cdf(d, 550), digits = 3))
println(round(ccdf(d, 550), digits = 3))
println(round(quantile(d, 0.95), digits = 1))

z = Normal()
p95 = cdf(z, 1.96) - cdf(z, -1.96)
println(round(p95, digits = 3))`,
      out: `0.841
0.159
582.2
0.95`,
      a: [
        "平均500・SD50の正規分布では、550以下になる確率は約84.1%、550を超える確率は約15.9%です。2つを足すと1になります。",
        "`quantile(d, 0.95)` は上位5%が始まる境界です。標準正規分布の -1.96 から 1.96 の間には約95%が入ることも、同じ `cdf` で確認できます。",
      ],
    },
    {
      t: "極端な確率は、logpdf・ccdf・logccdfで直接求める",
      b: [
        "極端な裾では、数学的に正しい式でも途中の丸めで情報を失います。標準正規分布の40における密度は正ですが、Float64の `pdf` では0.0へunderflowし、その対数は `-Inf` になります。`logpdf` は対数密度を直接計算するため有限値を保ちます。",
        "右裾確率も `1 - cdf(d, x)` と引き算せず、`ccdf(d, x)` を使います。cdfが1.0へ丸められた後では、1から引いても失った桁は戻りません。対数が必要なら `logccdf` を直接使います。",
      ],
      code: `using Distributions

d = Normal()
println(pdf(d, 40.0))
println(log(pdf(d, 40.0)))
println(round(logpdf(d, 40.0), digits = 3))

println(1 - cdf(d, 10.0))
println(ccdf(d, 10.0))
println(round(logccdf(d, 10.0), digits = 3))`,
      out: `0.0
-Inf
-800.919
0.0
7.619853024160498e-24
-53.231`,
      a: [
        "`0.0` と表示されたから事象が不可能なのではありません。標準正規で10を超える確率は約7.62×10⁻²⁴です。数値表現上の0と、確率モデル上の0を区別します。",
        "分布APIには `logpdf`、`logcdf`、`logccdf` が用意されています。自分で `log(pdf(...))` や `log(1-cdf(...))` を組み立てる前に、目的の尺度を直接返す公開APIを探します。",
      ],
    },
    {
      t: "ほぼ等しい数の差にはlog1p・expm1と式変形を使う",
      b: [
        "underflowとは逆に、1へ非常に小さい数を足すと、その差が丸めで消えることがあります。`log(1+x)` の代わりに `log1p(x)`、`exp(x)-1` の代わりに `expm1(x)` を使うと、xが0付近でも有効桁を保てます。",
        "近い2数を引くcancellation（桁落ち）も同じ問題です。`sqrt(x+1)-sqrt(x)` はxが大きいと0へ丸められますが、有理化した逆数の式なら小さな差を直接表現できます。",
      ],
      code: `x = 1e-16
println(log(1 + x))
println(log1p(x))
println(exp(x) - 1)
println(expm1(x))

large = 1e16
naive_difference = sqrt(large + 1) - sqrt(large)
stable_difference = 1 / (sqrt(large + 1) + sqrt(large))
println(naive_difference)
println(stable_difference)`,
      out: `0.0
1.0e-16
0.0
1.0e-16
0.0
5.0e-9`,
      a: [
        "式変形は数学の飾りではなく、どの桁をコンピュータに残すかを決めます。差分、分散、尤度、勾配の計算で非有限値や不自然な0が出たら、データを削る前に計算式のconditioningを点検します。",
        "分散を `mean(x.^2) - mean(x)^2` と1回の差で自作する方法も、大きな平均の周りに小さく散るデータで桁落ちしやすくなります。`Statistics.var` のような検証済み実装を優先します。",
      ],
    },
    {
      t: "確率の和とロジスティック尤度もlog空間で計算する",
      b: [
        "混合分布やBayes計算では、非常に小さい確率の和の対数が必要です。`log(exp(a)+exp(b))` は両方の指数が0へunderflowすると `-Inf` になります。最大値を先に引くlog-sum-exp変形なら有限値を保てます。",
        "ロジスティック回帰でも、ηが大きいと確率が1.0へ丸められ、失敗の `log(1-p)` が `-Inf` になります。softplusを使う `-softplus(η)` は同じ対数確率を安定に計算できます。GLMなどの検証済み実装が内部で安定化する理由です。",
      ],
      code: `logaddexp(a, b) =
    max(a, b) + log1p(exp(-abs(a - b)))

a, b = -1000.0, -1001.0
println(log(exp(a) + exp(b)))
println(logaddexp(a, b))

softplus(x) = max(x, 0) + log1p(exp(-abs(x)))
eta = 1000.0
p = 1 / (1 + exp(-eta))
println(p)
println(log(1 - p))
println(-softplus(eta))`,
      out: `-Inf
-999.6867383124818
1.0
-Inf
-1000.0`,
      a: [
        "ここでの `logaddexp` は有限な2値を説明する最小例です。実務では空配列、±Inf、NaN、多数要素も扱う必要があるため、使用する統計パッケージの検証済み関数とテストを優先します。",
        "予測確率が表示上1.000でも、現象が確実という意味ではありません。ロジット尺度の有限な値、モデル外挿、分離、較正を確認し、丸められた確率を確実性の主張へ変換しません。",
      ],
    },
    {
      t: "数値安定性を、結果解釈と再現性の監査項目にする",
      b: [
        "計算後に `all(isfinite, values)` を確認するだけでなく、0、±Inf、NaNがどの段階で生じたかを追います。非有限行の一括削除、確率への任意の小定数追加、係数の切り詰めは、推定対象を静かに変えるため既定の修正にしません。",
        "監査では、安定な専用APIへの置換、式の再配置、変数の中心化・尺度調整、入力順の変更、必要な箇所だけ `BigFloat` で再計算、理論上の恒等式や単調性の確認を行います。精度を上げても不適切なモデル、識別不能、悪いデザインは直りません。",
        "p値や信頼区間は未丸め値で判断し、表示だけを最後に丸めます。機械精度ほどの差で結論が変わる場合は、どちらか一方を断言するのではなく、効果量・区間・代替計算・ソフトウェア版を報告します。数値誤差に頑健でない結論は、科学的にも頑健ではありません。",
      ],
      a: [
        "長期的には、コードがエラーなく終了したことより、有限性、範囲、単調性、別表現との一致、精度を変えた感度をテストします。これらは後続の最尤法、ロジスティック回帰、混合モデル、シミュレーションへ共通する品質基準です。",
        "再現性の許容誤差は一律の桁数ではなく、量の尺度と意思決定への影響から決めます。CPU・並列削減順・BLASが変わって最下位桁が動いても結論が同じか、逆に小差を見逃していないかを両方向から点検します。",
      ],
    },
    {
      t: "分布名より生成過程を考える",
      b: [
        "1回の成功・失敗ならBernoulli、その独立な試行を決まった回数だけ足した成功数ならBinomial、一定区間に独立に起こる事象数ならPoissonが候補です。左右対称な連続値の誤差にはNormal、正で右裾を引き、対数を取ると正規分布に近い量にはLogNormalが候補になります。これは機械的な合否表ではなく、生成過程を言葉にする出発点です。",
        "反例も重要です。標準正規分布と、-1または1を半々で取る分布は、どちらも平均0・SD1です。それでも片方は滑らかな釣鐘型、もう片方は2点しか取りません。平均とSDが同じだから同じ分布、とは言えません。ヒストグラムや研究設計に戻って確かめます。",
        "そして分布は現実そのものではなく、問いに必要な特徴を残したモデルです。候補を作り、予測をデータと比べ、合わないところを診断する——この往復が、以後の推論とモデリングの土台になります。",
      ],
    },
  ],
  ex: [
    {
      k: "choice",
      q: "1人の正誤、20問中の正答数、1分間のエラー数に対応する分布の並びはどれでしょう?",
      opts: [
        "Bernoulli、Binomial、Poisson",
        "Binomial、Bernoulli、Normal",
        "Poisson、LogNormal、Bernoulli",
      ],
      ans: 0,
      why: "正誤は0/1の1試行、正答数は決まった回数の成功数、エラー数は区間内の発生回数です。サポートと生成過程の両方から候補を選びます。",
      hint: "1試行か、決まった回数の合計か、一定時間の回数かを順に見ます。",
    },
    {
      k: "fill",
      q: "平均500・SD50の正規分布で、反応時間が550以下となる確率を求めます。空欄〔?〕に入る関数名を入力しましょう。",
      code: `p = 〔?〕(Normal(500, 50), 550)`,
      accept: ["cdf"],
      show: "cdf",
      why: "`cdf(d, x)` は分布 `d` から生じる値が `x` 以下となる累積確率を返します。",
      hint: "cumulative distribution function の略です。",
      placeholder: "関数名",
    },
    {
      k: "tf",
      q: "確率分布について、それぞれ正しいか判定しましょう。",
      items: [
        {
          s: "連続分布では、`pdf(d, x)` の高さが値 `x` をちょうど取る確率である",
          a: false,
          why: "連続分布の `pdf` は確率密度です。一点の確率は0で、区間の確率は密度を面積として積み上げたものです。",
        },
        {
          s: "同じ成功確率を持つ独立なBernoulli試行の成功数は、Binomial分布で表せる",
          a: true,
          why: "Binomialは、決まった回数の独立なBernoulli試行における成功数の分布です。",
        },
        {
          s: "平均と標準偏差が同じ2つの変数は、必ず同じ分布を持つ",
          a: false,
          why: "平均とSDは分布の一部しか要約しません。標準正規分布と±1を半々で取る分布は、平均0・SD1でも形がまったく違います。",
        },
      ],
      hint: "3つとも、密度・生成過程・要約統計のどれを分布全体と取り違えていないか確認しましょう。",
    },
  ],
};
